# TODO

## 临时文件清理系统

当前临时图片文件（`~/.rcui/tmp/images/`）使用简单的 5 分钟延迟清理策略，需要实现一个完整的项目级清理能力。

### 需求

- **自动清理策略**
  - 按时间：清理超过 30 天的临时文件
  - 按大小：当 `~/.rcui/tmp/` 总大小超过阈值时触发清理（如 500MB）
  - 启动时清理：服务启动时自动扫描并清理过期文件

- **手动清理**
  - 提供 API 端点（如 `POST /api/settings/cleanup`）支持前端触发手动清理
  - CLI 命令支持（如 `rcui cleanup`）
  - 支持查询当前临时文件占用空间

- **配置化**
  - 清理策略可通过环境变量或数据库配置调整
  - 保留天数、大小阈值等参数可配置
